// var ref = $('.btn');
tippy('[title]', {
    arrow: false,
    placement: 'top',
    theme: 'mom',
    animateFill: false,
    animation: '',
    offset: '0, 10',
    duration: [0, 0]
    // trigger: 'click'
});

// var popper = new Tooltip(ref, {     placement: 'top-start',     title:
// "AHAHAH",     offset: 'top 10',     popperOptions: {         onCreate:
// function (data) {             console.log(data);         }     } });

function makeTooltip(ref) {}